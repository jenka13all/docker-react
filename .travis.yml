language: generic
sudo: required

services:
    - docker

before_install:
    - docker build -t jenka13all/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true jenka13all/docker-react npm run test